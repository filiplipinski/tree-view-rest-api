import { colors } from "./colors";

export const theme = {
  colors,
  font: {
    thin: 300,
    regular: 400,
    semi_bold: 600,
    bold: 700
  },
  media: {
    mobile: '@media (max-width: 1024px)'
  }
}